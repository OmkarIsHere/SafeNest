<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/icLocationPin"
        android:layout_width="@dimen/size_large_margin"
        android:layout_height="@dimen/size_large_margin"
        android:scaleType="fitCenter"
        android:contentDescription="Pin"
        android:src="@drawable/location_pin"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

    <com.google.android.material.imageview.ShapeableImageView
        android:id="@+id/imgPinIcon"
        android:layout_width="@dimen/size_less_max_large"
        android:layout_height="@dimen/size_less_max_large"
        android:scaleType="centerCrop"
        android:layout_marginBottom="@dimen/size_small"
        app:strokeWidth="@dimen/smallest_padding"
        app:shapeAppearanceOverlay="@style/circleImageView"
        android:contentDescription="User Icon"
        android:src="@drawable/person"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>